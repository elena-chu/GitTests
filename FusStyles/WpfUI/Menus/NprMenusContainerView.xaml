<UserControl x:Class="Ws.Fus.UI.Wpf.Menus.Views.NprMenusContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:extensions="http://schemas.insightec.com/wpf/"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

  <Border Background="{Binding ApplicationBackgroundBrush}">
    <Grid Style="{StaticResource MenuContainerStyle}">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Style="{StaticResource MenuGroupSeparationRowStyle}"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Style="{StaticResource MenuGroupSeparationRowStyle}"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition/>
        <ColumnDefinition/>
      </Grid.ColumnDefinitions>

      <Button
        Grid.Row="0"
        Grid.Column="0"
        Style="{StaticResource XStyle.Button.IconWithTextUnderneath}"
        extensions:IconedButton.Icon="{StaticResource XCanvas.Nrp.Manual}"
        Content="Manual NPR"/>
      
      <Button
        Grid.Row="0"
        Grid.Column="1"
        Style="{StaticResource XStyle.Button.IconWithTextUnderneath}"
        extensions:IconedButton.Icon="{StaticResource XCanvas.Nrp.Spherical}"
        Content="Spherical NPR"/>

      <Button
        Grid.Row="2"
        Grid.ColumnSpan="2"
        Margin="0,0,0,15"
        Content="Approve CT NPRs"
        Command="{Binding ViewModel.ApproveCTCommand}"
        Style="{StaticResource XStyle.Button.Primary.TextWithSideIcon}"
        extensions:IconedButton.Icon="{StaticResource XPath.DashedCircleCheckmark}"/>

      <Button
        Grid.Row="3"
        Grid.ColumnSpan="2"
        Content="Approve MRI NPRs"
        Command="{Binding ViewModel.ApproveMRICommand}"
        Style="{StaticResource XStyle.Button.Primary.TextWithSideIcon}"
        extensions:IconedButton.Icon="{StaticResource XPath.DashedCircleCheckmark}"/>

      <Expander x:Name="MoreExp" Grid.Row="5" Foreground="White" Grid.ColumnSpan="2">
        <Expander.Style>
          <Style TargetType="{x:Type Expander}">
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Header" Value="More"/>
            <Style.Triggers>
              <Trigger Property="IsExpanded" Value="True">
                <Setter Property="VerticalAlignment" Value="Bottom"/>
                <Setter Property="Header" Value="Less"/>
              </Trigger>
            </Style.Triggers>
          </Style>
        </Expander.Style>
        <Grid VerticalAlignment="Bottom">
          <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>
          <Grid.Resources>
            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource XStyle.Button.IconWithTextUnderneath}"/>
          </Grid.Resources>
          <Button
            Grid.Column="0"
            Grid.Row="0"
            extensions:IconedButton.Icon="{StaticResource XCanvas.Nrp.ResetSinuses}"
            Content="Reset Sinuses"/>
          <Button
            Grid.Column="1"
            Grid.Row="0"
            extensions:IconedButton.Icon="{StaticResource XCanvas.Nrp.ResetCalcifications}"
            Content="Reset Calcifications"/>
          <Button
            Grid.Column="0"
            Grid.Row="1"
            extensions:IconedButton.Icon="{StaticResource XCanvas.Nrp.ResetFolds}"
            Content="Reset Folds"/>
          <Button
            Grid.Column="1"
            Grid.Row="1"
            extensions:IconedButton.Icon="{StaticResource XCanvas.Nrp.CtThreshold}"
            Content="CT Threshold"/>
          <Button
            Grid.Row="2"
            Grid.ColumnSpan="2"
            Margin="0,15,0,0"
            Content="Re-acquire Fold Scan"
            Style="{StaticResource XStyle.Button.Secondary.TextWithSideIcon}"
            extensions:IconedButton.Icon="{StaticResource XCanvas.Nrp.ReacquireFolds}"/>
        </Grid>
      </Expander>
    </Grid>
  </Border>
</UserControl>
